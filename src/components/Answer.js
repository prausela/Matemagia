import { Button } from "react-bootstrap";
import React from "react";

const Answer = () => {
    return (
        <div className="answer d-grid">
            <Button variant="light" size="lg">Hola</Button>
        </div>
    );
}

export default Answer;